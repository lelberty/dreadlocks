package dreadlocks.core

import java.util.concurrent.locks.Lock
// import scala.collection.concurrent.TrieMap

class PairLock {

  // @volatile private val masterMap : TrieMap[Lock, Boolean] = new TrieMap[Lock, Boolean]()

//  private class LockData(a : Lock, b : Lock, 

//  def lock(a : Lock, b : Lock) : Unit = {
//
//
//  }
}
